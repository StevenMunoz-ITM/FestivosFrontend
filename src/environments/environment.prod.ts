// Configuración para ambiente de producción
export const environment = {
  production: true,
  urlBase: 'https://tu-servidor-produccion.com/api' // Cambiar cuando tengas el servidor en producción
};
